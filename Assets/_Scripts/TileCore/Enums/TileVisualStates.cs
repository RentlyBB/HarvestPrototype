namespace _Scripts.TileCore.Enums {
    public enum TileMainVisualStates {
        Default,
        Countdown,
        Collect,
        BadCollect,
    }

    public enum TileSubVisualStates {
        Unpressed,
        Pressed,
    }
}